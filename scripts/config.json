{
  "version": "0.2",
  "scripts": [
    {
      "documentation": {
        "command": "generate-account",
        "description": "Generates a migration for a new user/account. Run 'mu script registration generate-account --help' to see the options.",
        "arguments": []
      },
      "environment": {
        "interactive": false,
        "script": "generate-account/run.sh"
      },
      "mounts": {
        "app": "/data/app/"
      }
    }
  ]
}
